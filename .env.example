# Caillou Calendar — local environment variables
# Copy to `.env.local` and fill in for live integrations.
#
# Sentry error reporting. When unset, the app silently falls back to
# console-only error logging. Used by src/lib/errorReporting.ts.
VITE_SENTRY_DSN=
